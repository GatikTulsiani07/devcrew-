@AGENTS.md

# Shared Workspace Audit

`Guildly-Reference/audit.md` is the canonical UI research and design audit for this multi-worktree workspace. Every repository in the architecture shares this single document as its audit source of truth.

Future sessions must reference the shared workspace audit at `Guildly-Reference/audit.md`. They must never create, copy, or maintain repository-specific audit files.
